const header=document.querySelector('.site-header'),nav=document.querySelector('.nav'),menuToggle=document.querySelector('.menu-toggle'),navLinks=[...document.querySelectorAll('.nav-link')],sections=[...document.querySelectorAll('main section[id]')],reveals=document.querySelectorAll('.reveal'),parallaxItems=document.querySelectorAll('.parallax-bg'),magneticItems=document.querySelectorAll('.magnetic'),cursorDot=document.querySelector('.cursor-dot'),cursorRing=document.querySelector('.cursor-ring');document.getElementById('year').textContent=new Date().getFullYear();let ticking=false;function update(){header.classList.toggle('scrolled',scrollY>40);parallaxItems.forEach(item=>{const s=item.closest('section'),r=s.getBoundingClientRect(),o=(innerHeight/2-(r.top+r.height/2))*.055;item.style.transform=`translate3d(0,${o}px,0) scale(1.08)`});let current='inicio';sections.forEach(s=>{if(scrollY>=s.offsetTop-innerHeight*.38)current=s.id});navLinks.forEach(l=>l.classList.toggle('active',l.getAttribute('href')===`#${current}`));ticking=false}addEventListener('scroll',()=>{if(!ticking){requestAnimationFrame(update);ticking=true}},{passive:true});update();const observer=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');observer.unobserve(e.target)}}),{threshold:.13});reveals.forEach(x=>observer.observe(x));menuToggle?.addEventListener('click',()=>{const open=menuToggle.classList.toggle('open');nav.classList.toggle('open',open);menuToggle.setAttribute('aria-expanded',open)});navLinks.forEach(l=>l.addEventListener('click',()=>{nav.classList.remove('open');menuToggle?.classList.remove('open');menuToggle?.setAttribute('aria-expanded','false')}));if(matchMedia('(pointer:fine)').matches){let mx=innerWidth/2,my=innerHeight/2,rx=mx,ry=my;addEventListener('mousemove',e=>{mx=e.clientX;my=e.clientY;cursorDot.style.left=`${mx}px`;cursorDot.style.top=`${my}px`});function follow(){rx+=(mx-rx)*.16;ry+=(my-ry)*.16;cursorRing.style.left=`${rx}px`;cursorRing.style.top=`${ry}px`;requestAnimationFrame(follow)}follow();document.querySelectorAll('a,button,.service-card').forEach(el=>{el.addEventListener('mouseenter',()=>cursorRing.classList.add('active'));el.addEventListener('mouseleave',()=>cursorRing.classList.remove('active'))});magneticItems.forEach(item=>{item.addEventListener('mousemove',e=>{const r=item.getBoundingClientRect(),x=(e.clientX-r.left-r.width/2)*.1,y=(e.clientY-r.top-r.height/2)*.1;item.style.transform=`translate(${x}px,${y}px)`});item.addEventListener('mouseleave',()=>item.style.transform='')})}document.querySelectorAll('.service-card').forEach(card=>{card.addEventListener('mousemove',e=>{if(matchMedia('(pointer:coarse)').matches)return;const r=card.getBoundingClientRect(),rx=((e.clientY-r.top)/r.height-.5)*-4,ry=((e.clientX-r.left)/r.width-.5)*5;card.style.transform=`perspective(700px) rotateX(${rx}deg) rotateY(${ry}deg) translateY(-5px)`});card.addEventListener('mouseleave',()=>card.style.transform='')});
